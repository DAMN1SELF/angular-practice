import { NuevoFiltroPipe } from './nuevo-filtro.pipe';

describe('NuevoFiltroPipe', () => {
  it('create an instance', () => {
    const pipe = new NuevoFiltroPipe();
    expect(pipe).toBeTruthy();
  });
});
